package creatures;

public class Pet extends Animal {
    public Pet(String species, String name, Double weight, Integer age, Boolean alive) {
        super(species, name, weight, age, alive);
    }
}
